= form_for @blog_image, remote: true do |f|
  - if @blog_image.errors.any?
    #error_explanation
      %h2
        = pluralize(@blog_image.errors.count, "error")
        prohibited this blog image from being saved:
      %ul
        - @blog_image.errors.full_messages.each do |msg|
          %li= msg

  .field.grid_4
    = f.label :image
    = f.file_field :image
  .actions.grid_1
    = f.submit