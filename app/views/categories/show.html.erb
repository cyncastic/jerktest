<ul id="images">
<% @artworks.each do |artwork| %>
    <li class="grid_1">
        <a class="fancy" rel="group1" title="<%= artwork.title %>" href="<%= artwork.img_url %>"><img src="<%= artwork.thumb_url %>" /></a>
    </li>
<% end %>
</ul>